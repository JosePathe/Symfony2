{% extends 'MmiBlogBundle::layout.html.twig' %}

{% set memos = app.session.get('memos') %}

{% block content_content %}

{% for memo in memos %}
    <div class="alert alert-info" role="alert">
        {{ memo.message }}
        {{ memo.id }}
        <a href="{{ url('blog_memo_delete', {id:memo.id, target:'blog_memo_delete'}) }}" class="close" aria-label="Close" ><span aria-hidden="true">&times;</span></a>
    </div>
{% endfor %}

<a href="{{ url('blog_memo_delete_all') }}" class="btn btn-primary" role="button">Supprimer tous les mémos</a>

{% endblock %}